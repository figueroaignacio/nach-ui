'use client';

import { Typography } from '../../components/typography';

export function Default() {
  return (
    <Typography variant="p">
      This is a default paragraph typography rendering. It uses the default font size, line height,
      and paragraph spacing.
    </Typography>
  );
}
